# 不能使用Tab退格键
# k:(空格)v  -- 必须要有空格

# debug:
#  true

server:
  port: 8888
  servlet:
    context-path: /boot
  
spring:
  profiles:
    active: prod
  thymeleaf:
    cache: false  
  messages:
    basename: i18n.login   
  datasource:
    username: root
    password: zhangchao
    url: jdbc:mysql://localhost:3306/mydatabase?serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    
    initialSize: 5
    minIdle: 5
    maxActive: 20
    maxWait: 60000
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT 1 FROM DUAL
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
#   配置监控统计拦截的filters，去掉后监控界面sql无法统计，'wall'用于防火墙
    filters: stat,wall,logback
    maxPoolPreparedStatementPerConnectionSize: 20
    useGlobalDataSourceStat: true
    connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=500
    
mybatis:
  config-location: classpath:mybatis/mybatis-config.xml
  mapper-locations: classpath:mybatis/mapper/*.xml

logging:
  level:
    com.zhangchao: info
            #  测试缓存
    com.zhangchao.mapper: debug
  file: springboot.log

# 测试读取配置文件的值  ${random.value} ${random.int} ${random.value}
person:
  last-name: zhangsan${random.uuid}
  age: 12
  birth: 2017/12/15
  boss: false
  maps: {k1: v1,k2: 14}
  lists:
    - a
    - b
    - c
  dog:
    name: dog
    age: 15
    

---
server:
  port: 8083
spring:
  profiles: dev

---
server:
  port: 8084
spring:
  profiles: prod